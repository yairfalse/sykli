# This fixture is a placeholder — the actual test uses a sykli.go file
# The runner will need to handle this specially
IO.puts(~s({"version":"1","tasks":[{"name":"go_test","command":"echo go works"}]}))
