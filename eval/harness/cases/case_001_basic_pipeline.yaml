id: case_001
description: "Basic pipeline execution passes"
oracle_case: "001"
task_prompt: |
  ensure that sykli can execute a basic single-task pipeline defined in an
  Elixir SDK fixture that runs `echo hello`. The task should complete with
  exit code 0 and the overall pipeline should pass. Do not modify the oracle.
timeout_secs: 60
